export const environment = {
  production: false,
  apiUrl: '',             // Empty = use proxy.conf.json; set to full URL in production
  clerkPublishableKey: '', // Set in environment.prod.ts or via build-time replacement
};
